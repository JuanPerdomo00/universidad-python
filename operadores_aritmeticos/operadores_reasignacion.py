#!/usr/bin/python3

mi_var = 10
# es lo mismo que mi_var = mi_var + n donde n es el numero a usar con su operador
mi_var += 1  # sumar 1
print(mi_var)

# restar 1
mi_var -= 1  # 10
print(mi_var)

# y asi mismo con el resto de operadores aritmeticos
